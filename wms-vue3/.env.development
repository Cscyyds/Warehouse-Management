VITE_API_BASE_URL=/
VITE_API_PROXY_TARGET=https://www.aster-mindlink.cn
VITE_AI_PROXY_TARGET=http://127.0.0.1:8001
VITE_PAGE_AGENT_API_BASE_URL=/api/v1/page-agent
# PDF 审核工作台当前模式：本地调试——PDF 链路（上传/工作流/快照/知识库导入）
# 全部走同源请求，由 vite 代理 VITE_PDF_PROXY_TARGET 转发到本地 Coze_Connect 网关。
# VITE_PDF_API_BASE 置空：不把云端 7779 作为粘性寻址兜底源，避免调试请求被带上线。
# 需连云端时：VITE_PDF_API_BASE=https://www.aster-mindlink.cn:7779，
# 并把 VITE_PDF_PROXY_TARGET 改为 https://www.aster-mindlink.cn:7779。
VITE_PDF_PROXY_TARGET=http://127.0.0.1:8001
VITE_PDF_API_BASE=
